#include "interface.hpp"
// This file basically does nothing other than just include interface.hpp
// Excluding it causes the dll to not have any exported functions, so I'm
// keeping it here until I know a better option