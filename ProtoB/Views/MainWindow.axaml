<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:ProtoB.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:u="using:ProtoB.Controls"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="ProtoB.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="ProtoB">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

	<u:FooControl EnumerableBar="{Binding Tmp}"/>
	<!--ListBox Items="{Binding Tmp}">
		<ListBox.ItemTemplate>
			<DataTemplate>
				<TextBlock Text="{Binding Path=MyName, Mode=TwoWay}"/>
			</DataTemplate>
		</ListBox.ItemTemplate>
	</ListBox-->
	
</Window>